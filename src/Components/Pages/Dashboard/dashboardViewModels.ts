import { buildDashboardCommunityActionLabel } from './dashboardViewModelShared';
import {
  buildDashboardOverviewViewModel,
  buildOverviewSubtitle,
} from './dashboardOverviewViewModel';
import {
  buildDashboardConnectionsViewModel,
  buildConnectionsSubtitle,
} from './dashboardConnectionsViewModel';
import {
  buildDashboardCommunityViewModel,
  buildCommunitySubtitle,
} from './dashboardCommunityViewModel';
import {
  buildDashboardNotificationsViewModel,
  buildNotificationsSubtitle,
} from './dashboardNotificationsViewModel';

export {
  buildDashboardCommunityActionLabel,
  buildDashboardOverviewViewModel,
  buildOverviewSubtitle,
  buildDashboardConnectionsViewModel,
  buildConnectionsSubtitle,
  buildDashboardCommunityViewModel,
  buildCommunitySubtitle,
  buildDashboardNotificationsViewModel,
  buildNotificationsSubtitle,
};
